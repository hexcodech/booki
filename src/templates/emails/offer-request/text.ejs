Hi <%= offerer.nameDisplay %>,
<%= offerer.nameDisplay %> would like to buy your book <%= book.title %> for <%= offer.price %> CHF.
To respond, please click on the following link: <%= responseUrl %>.
<% if(requester.placeOfResidence) {%>
  <%= offerer.nameDisplay %>'s place of residence is <%= requester.placeOfResidence %>
<% } %>

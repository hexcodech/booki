Hi <%= offerer.nameDisplay %>,
<%= requester ? requester.nameDisplay : "An unregistered user" %> would like to buy your book <%= book.title %> for <%= offer.price %> CHF.
To respond, please click on the following link: <%= responseUrl %>.
<% if(request.message) {%>
	Message:
	<%= request.message %>
<% } %>
